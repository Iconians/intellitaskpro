import {
  TagManagerModalContent,
  type TagManagerModalProps,
} from "./TagManagerModalContent";

export function TagManagerModal(props: TagManagerModalProps) {
  return <TagManagerModalContent {...props} />;
}
